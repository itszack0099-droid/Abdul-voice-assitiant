name: Build Android APK

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.22.0"

      - name: Install Dependencies
        run: |
          cd abdul-ai
          flutter pub get

      - name: Build APK
        run: |
          cd abdul-ai
          flutter build apk --release

      - name: Build AAB
        run: |
          cd abdul-ai
          flutter build appbundle --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release-apk
          path: abdul-ai/build/app/outputs/flutter-apk/app-release.apk

      - name: Upload AAB
        uses: actions/upload-artifact@v4
        with:
          name: app-release-aab
          path: abdul-ai/build/app/outputs/bundle/release/app-release.aab
